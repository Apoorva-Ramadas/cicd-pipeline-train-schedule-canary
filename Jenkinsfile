pipeline {
    agent any
    environment {
        //be sure to replace "willbla" with your own Docker Hub username
        DOCKER_IMAGE_NAME = "apoorvaramadas1996/train-schedule"
    }
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
            } 
        }
    }
}
